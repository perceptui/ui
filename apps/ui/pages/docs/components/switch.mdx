import { Switch } from "@/components/percept-ui/switch";
import { IssueHeader } from "@/components/issue-header";
import { ComponentPreview } from "@/components/component-preview";
import { CommandsWrapper } from "@/components/commands-wrapper";
import { Callout } from "@/components/callout";

<h1 className="text-4xl font-bold mt-5">Switch</h1>
<p className="text-xl">Renders a Switch.</p>
<IssueHeader source="components/switch/switch" />

<ComponentPreview component={
  <div className="flex items-center justify-center p-2 w-96 mx-auto">
    <Switch/>
  </div>
    }>
```jsx code copy
import { Switch } from "@/components/percept-ui/switch"

export function SwitchDemo() {
  return (
    <Switch/>
  )
}
```
</ComponentPreview>

## Installation

### CLI

<CommandsWrapper
  description="Add component to your project."
  npmCommand={`npx @perceptui/cli@latest add switch`}
  yarnCommand={`npx @perceptui/cli@latest add switch`}
  pnpmCommand={`pnpm dlx @perceptui/cli@latest add switch`}
/>

### Import from `@perceptui/ui`

<Callout>
  **Note:** If you have installed `@perceptui/ui` package, then import Switch
  from `@perceptui/ui`.
</Callout>

```tsx code copy
import { Switch } from "@perceptui/ui";
```

<ComponentPreview component={
  <div className="flex items-center justify-center p-2 w-96 mx-auto">
    <Switch/>
  </div>
    }>
```jsx code copy
import { Switch } from "@perceptui/ui"

export function SwitchDemo() {
  return (
    <Switch/>
  )
}
```
</ComponentPreview>

## API References

| Prop      | Type     | Description         | Default |
| :-------- | :------- | :------------------ | :------ |
| className | `string` | className           | \_      |
