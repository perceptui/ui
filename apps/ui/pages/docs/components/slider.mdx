import { Slider } from "@/components/percept-ui/slider";
import { IssueHeader } from "@/components/issue-header";
import { ComponentPreview } from "@/components/component-preview";
import { CommandsWrapper } from "@/components/commands-wrapper";
import { Callout } from "@/components/callout";

<h1 className="text-4xl font-bold mt-5">Slider</h1>
<p className="text-xl">
  Used to allow users to make selections from a range of values.
</p>
<IssueHeader source="components/slider/slider" />

<ComponentPreview component={
  <div className="flex items-center justify-center p-2 w-96 mx-auto">
    <Slider/>
  </div>
    }>
```jsx code copy
import { Slider } from "@/components/percept-ui/slider"

export function SliderDemo() {
  return (
    <Slider/>
  )
}
```
</ComponentPreview>

## Installation

### CLI

<CommandsWrapper
  description="Add component to your project."
  npmCommand={`npx @perceptui/cli@latest add slider`}
  yarnCommand={`npx @perceptui/cli@latest add slider`}
  pnpmCommand={`pnpm dlx @perceptui/cli@latest add slider`}
/>

### Import from `@perceptui/ui`

<Callout>
  **Note:** If you have installed `@perceptui/ui` package, then import Slider
  from `@perceptui/ui`.
</Callout>

```tsx code copy
import { Slider } from "@perceptui/ui";
```

<ComponentPreview component={
<div className="flex items-center justify-center p-2 w-96 mx-auto">
    <Slider/>
  </div>    }>
```jsx code copy
import { Slider } from "@perceptui/ui"

export function SliderDemo() {
  return (
    <Slider/>
  )
}
```
</ComponentPreview>

## API References

| Prop      | Type     | Description         | Default |
| :-------- | :------- | :------------------ | :------ |
| color     | `string` | Color of the Slider | blue    |
| className | `string` | className           | \_      |

## Examples

### Color

<ComponentPreview component={
  <div className="flex flex-col gap-4 items-start justify-start px-3 p-4 bg-blue-500/15 mb-2 rounded-lg w-full">
    <div className="flex flex-col gap-4 items-start justify-start w-96 mx-auto">
    <Slider color="red"/>
    <Slider color="light"/>
    <Slider color="yellow"/>
    <Slider color="cyan"/>
    <Slider color="teal"/>
    <Slider color="emerald"/>
  </div>
  </div>
    }>
```jsx code copy
import { Slider } from "@/components/percept-ui/slider"

export function SliderColorsDemo() {
  return (
    <div className="flex flex-col gap-4 items-start justify-start px-3 p-4 bg-blue-500/15 mb-2 rounded-lg">
    <Slider color="red"/>
    <Slider color="light"/>
    <Slider color="yellow"/>
    <Slider color="cyan"/>
    <Slider color="teal"/>
    <Slider color="emerald"/>
  </div>
  )
}
```
</ComponentPreview>

<Callout>There are more color properties. Take your time to explore.</Callout>
