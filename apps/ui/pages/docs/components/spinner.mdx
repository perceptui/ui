import { Spinner } from "@/components/percept-ui/spinner";
import { IssueHeader } from "@/components/issue-header";
import { ComponentPreview } from "@/components/component-preview";
import { CommandsWrapper } from "@/components/commands-wrapper";
import { Callout } from "@/components/callout";

<h1 className="text-4xl font-bold mt-5">Spinner</h1>
<p className="text-xl">
  Used to allow users to make selections from a range of values.
</p>
<IssueHeader source="components/spinner/spinner" />

<ComponentPreview component={
  <div className="flex items-center justify-center p-2 w-96 mx-auto">
    <Spinner/>
  </div>
    }>
```jsx code copy
import { Spinner } from "@/components/percept-ui/spinner"

export function SpinnerDemo() {
  return (
    <Spinner/>
  )
}
```
</ComponentPreview>

## Installation

### CLI

<CommandsWrapper
  description="Add component to your project."
  npmCommand={`npx @perceptui/cli@latest add spinner`}
  yarnCommand={`npx @perceptui/cli@latest add spinner`}
  pnpmCommand={`pnpm dlx @perceptui/cli@latest add spinner`}
/>

### Import from `@perceptui/ui`

<Callout>
  **Note:** If you have installed `@perceptui/ui` package, then import Spinner
  from `@perceptui/ui`.
</Callout>

```tsx code copy
import { Spinner } from "@perceptui/ui";
```

<ComponentPreview component={
  <div className="flex items-center justify-center p-2 w-96 mx-auto">
    <Spinner/>
  </div>
    }>
```jsx code copy
import { Spinner } from "@perceptui/ui"

export function SpinnerDemo() {
  return (
    <Spinner/>
  )
}
```
</ComponentPreview>

## API References

| Prop      | Type                           | Description          | Default |
| :-------- | :----------------------------- | :------------------- | :------ |
| color     | `string`                       | Color of the Spinner | blue    |
| size      | `"xs"\|"sm"\|"md"\|"lg"\|"xl"` | Size of the Spinner  | md      |
| className | `string`                       | className            | \_      |

## Examples

### Color

<ComponentPreview component={
    <div className="flex flex-wrap gap-2 items-center justify-start px-3 w-full p-4 bg-blue-500/15 mb-2 rounded-lg">
    <Spinner color="red"/>
    <Spinner color="light"/>
    <Spinner color="yellow"/>
    <Spinner color="cyan"/>
    <Spinner color="teal"/>
    <Spinner color="emerald"/>
  </div>
    }>
```jsx code copy
import { Spinner } from "@/components/percept-ui/spinner"

export function SpinnerColorsDemo() {
  return (
      <div className="flex flex-wrap gap-2 items-center justify-start px-3 w-full p-4 bg-blue-500/15 mb-2 rounded-lg">
    <Spinner color="red"/>
    <Spinner color="light"/>
    <Spinner color="yellow"/>
    <Spinner color="cyan"/>
    <Spinner color="teal"/>
    <Spinner color="emerald"/>
  </div>
  )
}
```
</ComponentPreview>

<Callout>There are more color properties. Take your time to explore.</Callout>

### Size

<ComponentPreview component={
      <div className="flex flex-wrap gap-2 items-end justify-start px-3 w-full p-4 bg-slate-500/15 mb-2 rounded-lg">
    <Spinner size="xs"/>
    <Spinner size="sm"/>
    <Spinner size="md"/>
    <Spinner size="lg"/>
    <Spinner size="xl"/>
  </div>
    }>
```jsx code copy
import { Spinner } from "@/components/percept-ui/spinner"

export function SpinnerSizeDemo() {
  return (
     <div className="flex flex-wrap gap-2 items-end justify-start px-3 w-full p-4 bg-slate-500/15 mb-2 rounded-lg">
    <Spinner size="xs"/>
    <Spinner size="sm"/>
    <Spinner size="md"/>
    <Spinner size="lg"/>
    <Spinner size="xl"/>
  </div>
  )
}
```
</ComponentPreview>