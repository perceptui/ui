import { BounceLoader } from "@/components/percept-ui/bounce-loader";
import { IssueHeader } from "@/components/issue-header";
import { ComponentPreview } from "@/components/component-preview";
import { CommandsWrapper } from "@/components/commands-wrapper";
import { Callout } from "@/components/callout";

<h1 className="text-4xl font-bold mt-5">Bounce Loader</h1>
<p className="text-xl">
  Used to show loading states.
</p>
<IssueHeader source="components/Loader/Loader" />

<ComponentPreview component={
  <div className="flex items-center justify-center p-2 w-96 mx-auto">
    <BounceLoader/>
  </div>
    }>
```jsx code copy
import { BounceLoader } from "@/components/percept-ui/loader"

export function BounceLoaderDemo() {
  return (
    <BounceLoader/>
  )
}
```
</ComponentPreview>

## Installation

### CLI

<CommandsWrapper
  description="Add component to your project."
  npmCommand={`npx @perceptui/cli@latest add bounce-loader`}
  yarnCommand={`npx @perceptui/cli@latest add bounce-loader`}
  pnpmCommand={`pnpm dlx @perceptui/cli@latest add bounce-loader`}
/>

### Import from `@perceptui/ui`

<Callout>
  **Note:** If you have installed `@perceptui/ui` package, then import BounceLoader from `@perceptui/ui`.
</Callout>

```tsx code copy
import { BounceLoader } from "@perceptui/ui";
```

<ComponentPreview component={
  <div className="flex items-center justify-center p-2 w-96 mx-auto">
    <BounceLoader/>
  </div>
    }>
```jsx code copy
import { BounceLoader } from "@perceptui/ui"

export function BounceLoaderDemo() {
  return (
    <BounceLoader/>
  )
}
```
</ComponentPreview>

## API References

| Prop      | Type                           | Description          | Default |
| :-------- | :----------------------------- | :------------------- | :------ |
| color     | `string`                       | Color of the BounceLoader | blue    |
| className | `string`                       | className            | \_      |

## Examples

### Color

<ComponentPreview component={
    <div className="flex flex-wrap gap-2 items-center justify-evenly px-3 w-full p-4 bg-blue-500/15 mb-2 rounded-lg">
    <BounceLoader color="red"/>
    <BounceLoader color="light"/>
    <BounceLoader color="yellow"/>
    <BounceLoader color="cyan"/>
    <BounceLoader color="teal"/>
    <BounceLoader color="emerald"/>
  </div>
    }>
```jsx code copy
import { BounceLoader } from "@/components/percept-ui/bounce-loader"

export function BounceLoaderColorsDemo() {
  return (
      <div className="flex flex-wrap gap-2 items-center justify-evenly px-3 w-full p-4 bg-blue-500/15 mb-2 rounded-lg">
    <BounceLoader color="red"/>
    <BounceLoader color="light"/>
    <BounceLoader color="yellow"/>
    <BounceLoader color="cyan"/>
    <BounceLoader color="teal"/>
    <BounceLoader color="emerald"/>
  </div>
  )
}
```
</ComponentPreview>

<Callout>There are more color properties. Take your time to explore.</Callout>
