import { MusicLoader } from "@/components/percept-ui/music-loader";
import { IssueHeader } from "@/components/issue-header";
import { ComponentPreview } from "@/components/component-preview";
import { CommandsWrapper } from "@/components/commands-wrapper";
import { Callout } from "@/components/callout";

<h1 className="text-4xl font-bold mt-5">Music Loader</h1>
<p className="text-xl">
  Used to show loading states.
</p>
<IssueHeader source="components/Loader/Loader" />

<ComponentPreview component={
  <div className="flex items-center justify-center p-2 w-96 mx-auto">
    <MusicLoader/>
  </div>
    }>
```jsx code copy
import { MusicLoader } from "@/components/percept-ui/loader"

export function MusicLoaderDemo() {
  return (
    <Loader/>
  )
}
```
</ComponentPreview>

## Installation

### CLI

<CommandsWrapper
  description="Add component to your project."
  npmCommand={`npx @perceptui/cli@latest add music-loader`}
  yarnCommand={`npx @perceptui/cli@latest add music-loader`}
  pnpmCommand={`pnpm dlx @perceptui/cli@latest add music-loader`}
/>

### Import from `@perceptui/ui`

<Callout>
  **Note:** If you have installed `@perceptui/ui` package, then import MusicLoader from `@perceptui/ui`.
</Callout>

```tsx code copy
import { MusicLoader } from "@perceptui/ui";
```

<ComponentPreview component={
  <div className="flex items-center justify-center p-2 w-96 mx-auto">
    <MusicLoader/>
  </div>
    }>
```jsx code copy
import { MusicLoader } from "@perceptui/ui"

export function MusicLoaderDemo() {
  return (
    <MusicLoader/>
  )
}
```
</ComponentPreview>

## API References

| Prop      | Type                           | Description          | Default |
| :-------- | :----------------------------- | :------------------- | :------ |
| color     | `string`                       | Color of the MusicLoader | blue    |
| size      | `"xs"\|"sm"\|"md"\|"lg"\|"xl"` | Size of the MusicLoader  | md      |
| className | `string`                       | className            | \_      |

## Examples

### Color

<ComponentPreview component={
    <div className="flex flex-wrap gap-2 items-center justify-evenly px-3 w-full p-4 bg-blue-500/15 mb-2 rounded-lg">
    <MusicLoader color="red"/>
    <MusicLoader color="light"/>
    <MusicLoader color="yellow"/>
    <MusicLoader color="cyan"/>
    <MusicLoader color="teal"/>
    <MusicLoader color="emerald"/>
  </div>
    }>
```jsx code copy
import { MusicLoader } from "@/components/percept-ui/music-loader"

export function MusicLoaderColorsDemo() {
  return (
      <div className="flex flex-wrap gap-2 items-center justify-evenly px-3 w-full p-4 bg-blue-500/15 mb-2 rounded-lg">
    <MusicLoader color="red"/>
    <MusicLoader color="light"/>
    <MusicLoader color="yellow"/>
    <MusicLoader color="cyan"/>
    <MusicLoader color="teal"/>
    <MusicLoader color="emerald"/>
  </div>
  )
}
```
</ComponentPreview>

<Callout>There are more color properties. Take your time to explore.</Callout>

### Size

<ComponentPreview component={
      <div className="flex flex-wrap gap-2 items-end justify-start px-3 w-full p-4 bg-slate-500/15 mb-2 rounded-lg">
    <MusicLoader size="xs"/>
    <MusicLoader size="sm"/>
    <MusicLoader size="md"/>
    <MusicLoader size="lg"/>
    <MusicLoader size="xl"/>
  </div>
    }>
```jsx code copy
import { MusicLoader } from "@/components/percept-ui/music-loader"

export function MusicLoaderSizeDemo() {
  return (
     <div className="flex flex-wrap gap-2 items-end justify-start px-3 w-full p-4 bg-slate-500/15 mb-2 rounded-lg">
    <MusicLoader size="xs"/>
    <MusicLoader size="sm"/>
    <MusicLoader size="md"/>
    <MusicLoader size="lg"/>
    <MusicLoader size="xl"/>
  </div>
  )
}
```
</ComponentPreview>